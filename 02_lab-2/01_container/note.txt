# Run container
docker build -t api-pozos:v1 .
docker compose up -d

# tag and push
docker tag api-pozos:v1 <dockerhub username>/api-pozos:v1
docker login
docker push <dockerhub username>/api-pozos:v1

# delete application
docker compose down -v